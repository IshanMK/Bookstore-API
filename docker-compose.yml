version: "3"

services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"

  app:
    build: .
    depends_on:
      - mongodb
    ports:
      - "3000:3000"
    environment:
      MONGODB_URI: mongodb://mongodb:27017/bookstore
